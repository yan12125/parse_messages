#pragma once

#include <string>
using namespace std;

const string messages_schema = "(thread text, timestamp int, user text, content text, contentIndex int)";
const string messages_columns = "(thread,timestamp,user,content,contentIndex)";
