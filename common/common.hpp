#pragma once

#include <string>

const std::string messages_schema = "(thread text, timestamp int, user text, content text, contentIndex int)";
const std::string messages_columns = "(thread,timestamp,user,content,contentIndex)";
